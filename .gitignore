# OS / outils
Thumbs.db
Desktop.ini
.DS_Store
*.log

# Fichiers internes (notes de session)
SESSION_NOTES.md

# Config secrets (ne JAMAIS versionner)
# contient le mot de passe SMTP IONOS + destinataires internes
config/config.php
config/mail_config.php

# PHP / deps
vendor/
composer.lock
*.local

# Archive de déploiement
bdj-deploy.zip