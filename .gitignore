# OS / outils
Thumbs.db
Desktop.ini
.DS_Store
*.log

# Fichiers internes (notes de session)
SESSION_NOTES.md

# Config secrets (ne JAMAIS versionner)
# contient le futur token SMTP Postmark + destinataires internes
config/config.php
config/mail_config.php

# PHP / deps
vendor/
composer.lock
*.local